[
  {
    "context": "((Editor && vim_mode == normal) && !menu)",
    "bindings": {
      "g r r": "editor::FindAllReferences"
    }
  },
  {
    "context": "(VimControl && !menu)",
    "bindings": {
      "g nosymbol d": "editor::GoToDefinition"
    }
  }
]
