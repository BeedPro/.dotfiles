#!/bin/sh
set -eu

# tags: #app

add_root="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

sudo xbps-install -Sy \
    fuse-sshfs \
    fastfetch \
    flac \
    ImageMagick

"$add_root/pde"
"$add_root/mpd"
"$add_root/wireguard"
"$add_root/void-packages"
"$add_root/opencode"
