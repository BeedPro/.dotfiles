#!/bin/sh
set -eu

# tags: #app

command -v npm >/dev/null 2>&1 || { echo "npm is not installed. Run add/node first."; exit 1; }

add_root="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

sudo xbps-install -Sy \
    wireguard-tools \
    fuse-sshfs \
    fastfetch \
    flac \
    ImageMagick

"$add_root/pde"
"$add_root/mpd"
"$add_root/void-packages"
"$add_root/power"
"$add_root/ai"
