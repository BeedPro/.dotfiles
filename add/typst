#!/usr/bin/env bash
set -euo pipefail

if ! command -v cargo >/dev/null 2>&1; then
    echo "cargo is not installed. Run add/rust first."
    exit 1
fi

sudo xbps-install -Syu
sudo xbps-install -Sy typst

cargo install --git=https://github.com/antonWetzel/prettypst.git --locked
