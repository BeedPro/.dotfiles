#!/usr/bin/env bash
set -euo pipefail

add_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

if ! command -v flatpak >/dev/null 2>&1; then echo "flatpak is not installed. Run add/flathub first."; exit 1; fi

sudo xbps-install -Sy gucharmap xournalpp

"$add_root/pack/firefox"
"$add_root/pack/readers"
"$add_root/pack/media"
"$add_root/pack/flatseal"
"$add_root/pack/vorta"
"$add_root/pack/espanso"
"$add_root/pack/emacs"
