#!/usr/bin/env bash
set -euo pipefail

PACKAGES=(
  dex
  i3-wm
  i3blocks
  i3lock
  xssproxy
)

sudo apt purge -y "${PACKAGES[@]}" || true
sudo apt autoremove -y

echo "Removed i3 packages from add/i3"
