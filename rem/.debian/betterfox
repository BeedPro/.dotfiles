#!/bin/bash
set -euo pipefail

TARGET_DIR="${1:-$HOME/.mozilla/firefox}"

if [ -L "$TARGET_DIR/user.js" ]; then
  rm -f "$TARGET_DIR/user.js"
fi

if [ -L "$TARGET_DIR/chrome" ]; then
  rm -f "$TARGET_DIR/chrome"
fi

echo "Removed Betterfox links from $TARGET_DIR"
