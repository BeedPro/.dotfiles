#!/bin/bash
set -euo pipefail

if command -v uv >/dev/null 2>&1; then
  uv self uninstall >/dev/null 2>&1 || true
fi

rm -f "$HOME/.local/bin/uv" "$HOME/.local/bin/uvx"
rm -rf "$HOME/.cache/uv" "$HOME/.local/share/uv"

echo "Removed uv"
